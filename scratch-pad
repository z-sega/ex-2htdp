#lang htdp/isl

(define (build-row size 1-pos current)
  (cond
    [(> current size) '()]
    [else (cons (if (= current 1-pos) 1 0)
                (build-row size 1-pos (add1 current)))]))

(build-row 3 2 1)
