;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex-105) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; -- ex-105.rkt
;; Some program contains the following data definition:

; A Coordinate is one of:
; - a NegativeNumber
; *interp.* on the y axis, distance from top
; - a PositiveNumber
; *interp.* on the x axis, distance from left
; - a Posn
; *interp.* an ordinary Cartesian point

;; Make up at least two data examples per clause in the data
;; definition. For each of the examples, explain its meaning
;; with a sketch of a canvas.

;; -> Check sketch-pad
